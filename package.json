{
  "name": "add-name-affixes",
  "version": "1.0.1",
  "description": "Add prefix and suffix to the basename of file paths or URLs, zero dependencies.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "keywords": ["filename", "prefix", "suffix", "basename", "path", "url", "utility", "no-deps"],
  "author": "lineCode",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.1.3",
    "vitest": "^0.32.2"
  }
}
